// import styled from "styled-components";

// export const StyledDiv = styled.div`
// display : ${({Table, Row, Cell}) => {Table ? 'table' : Row ? 'table-row' : Cell ? 'table-cell' : ''}};
// text-align: center;
// border: ${props => props.Cell ? 'solid thin black' : ''};
// font-weight: ${props => props.Heading ? 'bold' : ''};
// padding: ${props => props.Cell ? '10px' : ''};`